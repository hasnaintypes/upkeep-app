// Public API of the projects feature. Import from "@/features/projects" instead
// of reaching into internal files (components/lib/constants/types) directly.
//
// components/ doesn't exist yet -- it'll be added by whichever Phase 2 issue
// builds the first actual UI, per this repo's "only add subfolders with real
// content" convention.

export * from "./constants";
export * from "./lib/actions";
export * from "./types";
